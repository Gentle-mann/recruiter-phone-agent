# Production Convex deployment. The client URL is public and safe to commit.
# CONVEX_DEPLOYMENT lets `npm run deploy` and `npm run seed` target production.
CONVEX_DEPLOYMENT=prod:rapid-dodo-713
VITE_CONVEX_URL=https://rapid-dodo-713.convex.cloud
